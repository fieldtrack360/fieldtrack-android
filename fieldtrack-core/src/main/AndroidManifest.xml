<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Everything the SDK needs is declared here and merges into the host APK, so an
      integrating app adds nothing to its own manifest (PERMISSIONS.md §8 item 6).
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Deliberately NOT declared: REQUEST_IGNORE_BATTERY_OPTIMIZATIONS. It is Play-policy
      sensitive and must be the host's explicit choice, never auto-requested (EC-15).
    -->

    <application>

        <service
            android:name=".service.TrackingService"
            android:exported="false"
            android:foregroundServiceType="location"
            android:permission="android.permission.FOREGROUND_SERVICE_LOCATION"
            android:stopWithTask="false" />

        <!-- Every receiver states android:exported explicitly; relying on the
             platform default is how the reference ended up with an ambiguous one. -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!--
          The one revival path that is not a WorkManager job. See ServiceHeartbeat: on a
          MIUI/HyperOS device left at the default "Restricted" battery setting, JobScheduler
          does not run the backstop or the restore worker at all (EC-22), and AlarmManager
          is the independent subsystem that still delivers.

          No SCHEDULE_EXACT_ALARM is declared with it, deliberately and for the same reason
          REQUEST_IGNORE_BATTERY_OPTIMIZATIONS is not (EC-15): it is Play-policy reviewed,
          and an SDK must not make that submission decision for its host. The alarm is
          inexact unless the HOST declares the permission itself, which ServiceHeartbeat
          detects at runtime.
        -->
        <receiver
            android:name=".service.ServiceHeartbeatReceiver"
            android:exported="false" />

        <receiver
            android:name=".motion.ActivityTransitionReceiver"
            android:exported="false" />

        <receiver
            android:name=".motion.StationaryFenceReceiver"
            android:exported="false" />

    </application>

</manifest>
